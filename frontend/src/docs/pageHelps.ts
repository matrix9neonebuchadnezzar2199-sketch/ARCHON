/** 各ルート用の解説文（日本語・Markdown）。`PageDoc` で表示。 */

export const PAGE_HELP: Record<string, string> = {
  "/": `## この画面
ダッシュボードは、**バックエンドの生死**と、ARCHON に組み込まれた3系統（**AI Hedge Fund** / **トレーディングエージェント** / **Ultimate**）の利用可否を一目で示します。

## 使い方
- 各カードの **利用可/未利用** は、\`vendors\` 以下のサブモジュールが正しくロードできたかに依存します。
- **システム状態** のバージョン・ステータスは、API \`/api/archon/health\` の結果を表示します。

## 制限事項
- 実際の**売買**は行いません。学習・研究目的のシミュレーション想定です。`,

  "/ultimate": `## Ultimate モードとは
**13 名の Guru 投資家**（AI Hedge Fund 系）のシグナルと、**トレーディングエージェント**（アナリスト・投資/リスク討議等）の結果を**1本のパイプライン**で合わせ、**銘柄ごと**に最終的な**買い / 売り / ホールド**方針を出します。  
Guru 側の多数決と、TA 側の議論内容を**融合**するのがこのモードの役割です。

## 画面の主な欄
- **銘柄** … カンマ区切りで複数可能（例: \`AAPL,MSFT\`）。英大文字に正規化して送信。
- **取引日** … その日付を基準に、データ取得・文脈が揃います（エンジン定義に準拠）。
- **Guru** … 現在の設定で有効な投資家ペルソナ一覧。設定画面で有効/無効を切り替え可。
- **アナリスト** … TA 側の役割名（市場、ニュース等）。**Guru 投票の内訳**・**TradingAgents の判断**・**討議（投資/リスク）** 等を段階的に表示します。

## 実行中のテキストについて
黒枠内の**英語の進行メッセージ**は、バックエンドが SSE で流す**内部ログ**です。UI 文言とは別層のため、必要なら設定の**出力言語**と併せて、応答本文がどの言語になるかはプロンプト/モデルに依存します。

## 失敗しやすい点
- **LLM 未接続**（API キー未設定、プロバイダ不一致）… 画面最上部の注意バナー、または **設定** で解消。  
- **Ollama　のみ** 使う場合は、プロバイダを \`ollama\` にし、ローカルにモデルを起動。`,

  "/hedge-fund": `## AI Hedge Fund 相当モード
**投資家ペルソナ**（Buffett, Munger 等）＋**機能系アナリスト**（ファンダ・テク等）に**並列**で任せ、銘柄ごとに**シグナル・信頼度・理由**を出します。TradingAgents より「**多様な投資哲学**」の寄せ集め色が強いです。

## 入力
- **銘柄** … 複数可。**開始日 / 終了日** はエンジン内で参照される期間です。
- **アナリスト** … タグを押して**有効/無効**切替。選択ゼロで実行不可。

## 表示
- **ポートフォリオの判断** … 集計結果（テキスト/構造体）。
- **アナリストシグナル** … 役割（タブ）→ 銘柄別に **強気/弱気/中立** バッジと**信頼度**と**要約**。

## 他モードとの違い
- ここは **ディベート形式ではなく**、主に**独立シグナルの集合**として読むイメージです。`,

  "/trading-agents": `## トレーディングエージェント相当モード
Tauric **TradingAgents** 系の流れ（アナリスト → **Bull/Bear 等** → リスク → 最終判断）を**1銘柄または複数**に対して走らせます。バックエンド定義上の**討議・記憶**（設定で有効時）の恩恵を主に受ける画面です。

## 入力
- **銘柄** … カンマ区切り。**取引日** 基準で日付揃え。
- 下部のバッジは、利用可能な**アナリスト職**の一覧（情報表示）。

## 出力ブロック
- **最終判断** … テキストまたは JSON。
- **アナリストレポート** … 役割別タブ。
- **投資討議** … Bull/Bear 等の対話的出力（中身は JSON/テキスト）。

## 注意
- 実行に **API 接続** が必要（クラウド or Ollama）。**メモリ** タブに蓄積される条件は、エンジンに「記憶あり」の経路を通した場合などに依存。`,

  "/backtest": `## バックテスト
**過去区間**で、選択した**アナリスト/ペルソナ**を用いてポートフォリオ的なシミュレーション（エンジン実装に従い）を行い、**損益曲線**と**主な指標**を表示します。

## パラメータ
- **銘柄** … カンマ区切り。
- **開始 / 終了** … 期間。データ取得の有効範囲はマーケット/データ元に左右。
- **初期資金（USD）** … シミュレーション資本。内部で**整数**扱いに正規化されることあり。
- **アナリスト** … タグ切替。対象者ゼロでは実行不可。

## 指標
- **累積リターン / 最終評価額 / 取引日数**
- **シャープ・ソルティノ**（データが出れば）
- **最大 DD** … ドローダウン（深さと日付があれば表示）

## 限界
- バックテストは**モデル・手数料・流動性等を理想化**しがちです。**実運用の損失リスク**には一切保証しません。`,

  "/portfolio": `## ポートフォリオ
バックエンド管理の**仮想ポジション**（現金・建玉）を**一覧**します。エンジン実行の結果を取り込んだ**最新**が反映される想定です（実装定義に準拠）。

## 表の列
- **買建 / 売建** … 口数
- **価格 / 時価** … 参照した市場価格に基づき**含み**評価
- **含み損益 / ウエイト** … 内訳把握用

## 操作
- **更新** … 直近のポートフォリオ再取得
- **リセット** … 仮想ポジション等を**初期化**（サーバ側。取り戻しに注意）

## 注意
**本番口座**とは無関係です。`,

  "/memory": `## メモリ
TradingAgents 系の **BM25** ベースの**状況→推奨**の記憶。検索用と**エージェント用**の両方のイメージで、**永続**（保存/読込/クリア）が可能な場合があります。

## 操作
- **ディスクに保存 / ディスクから読込** … ファイルベース（Docker なら**ボリューム**上に保存可）。
- **全削除** … 記憶を一括消去。取り戻しに注意。
- **状況 + メモリ種別 + 検索** … 近い**過去ケース**を**スコア**付きで列挙。

## 用語
- **稼働** … 現在のプロセス上の件数
- **保存** … ディスクに書き出された件数（実装定義）`,

  "/logs": `## ログ
**各エンジン**が生成した**実行単位**の**JSON ログ**（ファイル等）の**一覧**と、選択時の**詳細**閲覧です。フィルタでエンジン種を絞れます。

## 操作
- 左へ一覧 → クリックで右**詳細**
- **削除** … 当該ログ行を**物理削除**（戻れない想定）
- サイズ欄に **KB** 表示

## 用途
- 不具合時の**再現材料**
- 実行日・銘柄の**突き合わせ**`,

  "/settings": `## この設定画面
**UI 上で**以下をまとめて扱います。うち**API キー**は、保存時にサーバが**環境変数に適用**し、\`.memory/ui_secrets.json\` に**永続**します（\`.env\` を手で書かなくても動作可能。Docker では **.memory ボリューム**上）。

### LLM
- **プロバイダ**（openai / anthropic / google / xai / groq / deepseek / ollama 等）と、**モデル3種**＋**出力言語**。
- **討議ラウンド** … 投資・リスク系の**やり取り**の最大回数（目安、エンジン定義内）。

### API キー（主にクラウド用）
- **空欄のまま保存**すると、そのキーは**変更しません**（従来の .env または前回 UI 保存分を保持）。
- **Ollama** 選択時は原則**不要**（ローカル起動必須）。

### 有効化オプション
- **バリュエーション** エージェント
- **メモリ** / **振り返り**
- **ショート** 許可

### 数値
- **初期現金** / **マージン** … シミュレーション/ポートに関わるパラメータ

### Gurus / アナリスト
- チェックで**有効**な Guru・TA アナリスト集合を**その場で**更新。

## 取り扱い注意
- API キーは**厳重に管理**（共有端末、画面共有、ログ掲載に注意）。  
- **再起動**はしなくても多くの値は**即反映**、ただし一部ベンダライブラリは**初回 import** 時点の環境を掴むため、不整合時は**バックエンド再実行**を推奨。

## 補足: フィールド対応

| 区分 | 内容 |
| ---- | ---- |
| LLM プロバイダ | 利用基盤（**キーと整合**要確認） |
| 出力言語 | プロンプト/応答の**言語**の目安 |
| 各 API キー | **空欄で保存＝変更なし**（.env または前回の UI 保存を維持） |`,
};

/** 未登録ルート用 */
export const DEFAULT_PAGE_HELP = `## この画面
当該 URL の専用ドキュメントは未整備の場合があります。**設定** や**ダッシュボード**から目的の機能に移動してください。`;

export function getPageHelp(pathname: string): string {
  return PAGE_HELP[pathname] ?? DEFAULT_PAGE_HELP;
}
